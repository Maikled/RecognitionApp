<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RecognitionApp.View.GeneralPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecognitionApp.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="itemsControlStyle" TargetType="ItemsControl">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </Page.Resources>
    
    <NavigationView x:Name="GeneralNavigationView" ItemInvoked="NavigationView_ItemInvoked" Loaded="NavigationView_Loaded" IsBackButtonVisible="Collapsed">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Новый диалог" Icon="Add" Tag="RecognitionApp.View.AudioRecordingPage"/>
            <NavigationViewItemHeader Content="История"/>
            <!--<ItemsControl ItemsSource="{x:Bind _viewModel.RecognitionResults}" Style="{StaticResource itemsControlStyle}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <NavigationViewItem Content="{Binding FileName}" Icon="OpenFile"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>-->
        </NavigationView.MenuItems>

        <ScrollViewer>
            <Frame x:Name="ContentFrame" IsTabStop="True"/>
        </ScrollViewer>
    </NavigationView>
</Page>
